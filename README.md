To run the programm you have to install pympler

pip install pympler

Pympler is a development tool to measure, monitor and analyze the memory behavior of Python objects in a running Python application.
